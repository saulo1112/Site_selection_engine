# Plantilla de secrets para Streamlit Community Cloud (copiar a secrets.toml o pegar
# en la config de secrets del despliegue). NO comitear secrets.toml real.
#
# URL publica de la API FastAPI. Si se deja vacio, la app cae a leer los parquet
# locales directamente (fallback para correr todo en un solo proceso).
API_BASE_URL = "https://site-selection-api.onrender.com"
